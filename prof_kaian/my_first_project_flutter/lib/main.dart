import 'package:flutter/cupertino.dart';
import 'package:my_first_project_flutter/myapp.dart';

main() => runApp(const MyApp()); //a main vai retornar o runApp passando uma classe

// => essa flecha é a mesma coisa que { return o_que_ela_aponta }